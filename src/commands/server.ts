import { Command } from "@aeroware/aeroclient/dist/types";

export default {
    name: "server",
    args: false,
    usage: "[section] [subsection] [arguments]",
    async callback({}) {},
} as Command;
